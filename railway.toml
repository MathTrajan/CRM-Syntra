[build]
builder = "NIXPACKS"

[deploy]
startCommand = "java -jar target/syntra-0.0.1-SNAPSHOT.jar"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[healthcheck]
path = "/login"
timeout = 300
